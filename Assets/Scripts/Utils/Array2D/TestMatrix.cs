using UnityEngine;

namespace Utils.Array2D
{
    public class TestMatrix : MonoBehaviour
    {
        /*public Vector2Int dimension;
        private Matrix<CellTest> _matrix;
        
        private void Assign()
        {
            _matrix = new Matrix<CellTest>(dimension.x, dimension.y);
            for (var y = 0; y < dimension.y; y++)
            {
                for (var x = 0; x < dimension.x; x++)
                {
                    _matrix[x, y] = new CellTest(new Vector2Int(x, y));
                }
            }
        }

        private void Print()
        {
            Debug.Log($"Matrix[{dimension.x},{dimension.y}] = {_matrix[dimension.x,dimension.y].Position.ToString()}");
        }
        
        private void PrintCol()
        {
            Debug.Log($"left = {_matrix.Columns[dimension.x][dimension.y].GetNeighbor(Direction.Left, _matrix)?.Position.ToString()}");
            Debug.Log($"right = {_matrix.Columns[dimension.x][dimension.y].GetNeighbor(Direction.Right, _matrix)?.Position.ToString()}");
            Debug.Log($"top = {_matrix.Columns[dimension.x][dimension.y].GetNeighbor(Direction.Top, _matrix)?.Position.ToString()}");
            Debug.Log($"bottom = {_matrix.Columns[dimension.x][dimension.y].GetNeighbor(Direction.Bottom, _matrix)?.Position.ToString()}");
        }
        
        private void PrintRow()
        {
            Debug.Log($"Matrix[{dimension.x},{dimension.y}] = {_matrix.Rows[dimension.y][dimension.x].Position.ToString()}");
        }*/
        
    }
}